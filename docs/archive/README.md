# Documentation Archive

This folder keeps historical design inputs that are no longer the primary
documentation path but are still useful for traceability.

## Blueprints

`blueprints/quant_pipeline_blueprint_v1_4.*` is preserved as the historical
implementation blueprint that preceded the current code-checked docs.

The older generated `quant_pipeline_blueprint_v1_3.html` export was removed
because it was superseded by v1.4 and had no active references.
